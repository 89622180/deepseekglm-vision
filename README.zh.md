# deepseekglm-vision

????? OpenAI ????????????????????? `model` ?????????????????

## ????

???????????????????

- `GLM-5.1`
- `GLM-5`
- `deepseek-v4-pro`
- `deepseek-v4-flash`

??????????????????????????????? `glm-5.1`?`GLM-5.1`?`Glm-5.1`????????????? `model` ?????

`gpt-5.4`

??????????? `model` ????????????????????????????

????????????????????????? `role=user` ????????????????????????????????? `gpt-5.4`?

???????

| ?? | ?? | ?? |
| --- | --- | --- |
| ???? | `ROUTE_MODE=default` | ?????????????????????? `DEFAULT_BACKEND_BASE_URL`??? key ????? key?`model` ?? `gpt-5.4`?????????????????????????? key? |
| ???????? | `ROUTE_MODE=custom-vision` | ?????????????????????? `VISION_BACKEND_BASE_URL`?key ?? `VISION_BACKEND_API_KEY`????????? key?`model` ???? `gpt-5.4`???????????????????????? |
| ????? | `ROUTE_MODE=custom-all` | ??????? `model`?????????? `CUSTOM_BACKEND_BASE_URL`?key ?? `CUSTOM_BACKEND_API_KEY`????????? key????????????????? |

? `Authorization` ?????? + ??????? `model` ???? body ?????????`stream: true` ???????????

## ?? URL ??

?? OpenAI ? `image_url` ?????????????????????????????? JSON ?????????????????????????? `model` ?? `gpt-5.4`?

```json
{
  "model": "GLM-5.1",
  "messages": [
    {
      "role": "user",
      "content": [
        { "type": "text", "text": "??????" },
        {
          "type": "image_url",
          "image_url": {
            "url": "https://example.com/image.png",
            "detail": "high"
          }
        }
      ]
    }
  ]
}
```

??? OpenAI ??? base64 data URL?

```json
{
  "type": "image_url",
  "image_url": {
    "url": "data:image/png;base64,iVBORw0KGgo..."
  }
}
```

## ????

```bash
cp .env.example .env
npm start
```

???? `0.0.0.0:8080`???????

```env
DEFAULT_BACKEND_BASE_URL=http://127.0.0.1:8090/v1
VISION_BACKEND_MODEL=gpt-5.4
```

?????

```bash
curl http://127.0.0.1:8080/health
```

?????

```bash
curl http://127.0.0.1:8080/v1/chat/completions \
  -H "Authorization: Bearer sk-test" \
  -H "Content-Type: application/json" \
  -d '{"model":"GLM-5.1","messages":[{"role":"user","content":"??"}]}'
```

?????????????????? `model` ? `gpt-5.4`?????????? `model`?

## ????

?????

```env
ROUTE_MODE=default
DEFAULT_BACKEND_BASE_URL=http://127.0.0.1:8090/v1
VISION_BACKEND_MODEL=gpt-5.4
```

?????????

```env
ROUTE_MODE=custom-vision
DEFAULT_BACKEND_BASE_URL=http://127.0.0.1:8090/v1
VISION_BACKEND_BASE_URL=https://vision-api.example.com/v1
VISION_BACKEND_API_KEY=sk-vision
VISION_BACKEND_MODEL=gpt-5.4
```

??????

```env
ROUTE_MODE=custom-all
DEFAULT_BACKEND_BASE_URL=http://127.0.0.1:8090/v1
CUSTOM_BACKEND_BASE_URL=https://api.example.com/v1
CUSTOM_BACKEND_API_KEY=sk-custom
CUSTOM_BACKEND_MODEL=
```

?? `CUSTOM_BACKEND_MODEL` ?????????????? `model`????????????????? `model` ????????????

## ????????

?????

```bash
/www/wwwroot/deepseekglm-vision
```

?????

```bash
mkdir -p /www/wwwroot/deepseekglm-vision
cd /www/wwwroot/deepseekglm-vision
cp .env.example .env
npm test
npm start
```

systemd ???

```ini
[Unit]
Description=deepseekglm-vision middleware
After=network.target

[Service]
Type=simple
WorkingDirectory=/www/wwwroot/deepseekglm-vision
Environment=NODE_ENV=production
ExecStart=/usr/bin/node src/server.js
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```

??? `/etc/systemd/system/deepseekglm-vision.service` ??

```bash
systemctl daemon-reload
systemctl enable --now deepseekglm-vision
systemctl status deepseekglm-vision
```
